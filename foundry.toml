[profile.default]
src = "src"
out = "out"
libs = ["lib"]
solc_version = "0.8.24"
optimizer = true
optimizer_runs = 200


# Fork testing
[profile.fork]
url = "https://bsc-dataseed.binance.org"
block_number = 44000000  # Optional: pin to specific block

# Test configuration
[profile.default.fuzz]
runs = 256

[profile.default.invariant]
runs = 256
depth = 15
